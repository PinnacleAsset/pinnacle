<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Support</title>
  <%- include("partials/_seo"); -%>
  <script defer src="../JS/auth.js"></script>

</head>

<body class="bg-offWhite text-xs md:text-sm xl:text-base main">
  <%- include("partials/_header", { userName: user.fullName }); -%>
  <%- include("partials/_userSidebar", { page: currentPage }); -%>
  <%- include('partials/_alert') %>

  <p class="m-4 font-bold text-lightBlack text-lg md:text-xl lg:text-2xl xl:text-3xl">Support</p>
  <section class="p-4">
    <div class="bg-white mt-4 p-4 border border-[#767680] border-opacity-10 rounded-2xl">
        <p class="font-bold text-lightBlack text-sm md:text-base xl:text-lg">Help Desk</p>
        <p class="text-darkGreen">Contact our high-priority support team.</p>
  
        <form id="myForm" action="/user/support" method="POST" class="mt-10 w-full text-left">
            <%- include('partials/_input.ejs', { type:'text', id: 'heading', name: 'heading', label: 'Heading', placeholder: 'Your compliant heading', required: true }) %>
          <label for="body" class="font-medium text-gray-700">Body</label>
          <textarea name="body" id="body" class="mt-1 px-4 py-3 border border-white focus:border-primaryGreen rounded-md focus:outline-none w-full h-56 resize-none" placeholder="Enter your complaint or enquiry"></textarea>
          <%- include('partials/_button.ejs', { type: 'submit', label: 'Contact Support', icon: '❓', disabled: false }) %>
        </form>
      </div>
  </section>
</body>

</html>